server:
  servlet:
    context-path: /admin
  port: 80
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cskaoyan_mall_db?serverTimezone=UTC&
    username: root
    password: ss7832459
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

mybatis:
  type-handlers-package: com.handler
